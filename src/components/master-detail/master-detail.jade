mixin master(options)
    - options = options || {};
    - options['columns'] = options.columns || 3;
    - options['gridSize'] = options
    .master(class='col-md-#{options.columns}')
        block

mixin detail(options)
    - options = options || {};
    - options['columns'] = options.columns || 9;
    .detail.fluid-container
        block

mixin master-detail
    .master-detail
        block


mixin sidebar-btn
     .sidebar-btn
        block
