mixin view
    .view.fluid-container
        block

mixin app-view(options)
    - options = options || {};
    body.app-view
        block

mixin view-header(options)
    - options = options || {};
    - options['classes'] = options.classes || '';
    .view-header
        block

mixin view-content
    .view-content
        block
