mixin media(options)
    - options = options || {};
    - options['option1'] = options.option1 || NULL;
    - options['option2'] = options.option2 || NULL;
    .media
        block

mixin media-body(options)
    - options = options || {};
    - options['option1'] = options.option1 || NULL;
    - options['option2'] = options.option2 || NULL;
    .media-body
        block
