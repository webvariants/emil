
mixin table-cell(options)
    //- Set default values
    - options             = options          || {};
    - options['classes'] = options.classes || '';

    li.table-cell(data-id=options.dataID, class=options.classes)
        block

mixin nav-table-cell(options)
    //- Set default values
    - options             = options          || {};
    - options['classes'] = options.classes || '';

    li.nav-table-cell(class=options.classes)
        block