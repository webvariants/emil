//- Options
    label     => String containing the text to show in the label
    id        => Element id
    classes   => Element classes
    translate => translate label true/false
    value     => value next to label

mixin label(options)
    - options              = options              || {};
    - options['label']     = options.label        || '';
    - options['id']        = options.id && options.id.length ? options.id : null;
    - options['classes']   = options.classes      || '';
    - options['size']      = options.size         || '';
    span.emil-label(class='#{options.classes} #{options.size}', id=options.id) #{options.label}
